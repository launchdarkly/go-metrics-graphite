dependencies:
  pre:
    - rm -rf ~/.go_workspace/src/github.com/launchdarkly/go-metrics-graphite
    - mkdir -p ~/.go_workspace/src/github.com/launchdarkly/
    - ln -s ~/go-metrics-graphite ~/.go_workspace/src/github.com/launchdarkly/go-metrics-graphite
    - go version
  override:
    - go build

test:
  override:
    - go test
